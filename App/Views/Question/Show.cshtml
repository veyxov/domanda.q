@model IEnumerable<App.Models.Question>

@if (Model.Count() >= 1) 
{
    @foreach (var i in Model)
    {
        <h1> @Html.Raw(@i.Heading) </h1>
        <div class="jumbotron">
            @Html.Raw(@i.Text)
        </div>
    }
}
else
{
    <h1> No questions ! </h1>
}
